server:
  port: 8080
  error:
    include-message: always


cors:
  allowed-origins: '*'
  allowed-methods: '*'

spring:
  datasource:
    url: jdbc:postgresql://containers-us-west-203.railway.app:7114/railway
    username: postgres
    password: InF4CmMKwWVeMo9kPmHG
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

